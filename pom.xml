<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>net.enelson</groupId>
    <artifactId>SopParty-parent</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>SopParty Parent</name>

    <modules>
        <module>sop-party-protocol</module>
        <module>sop-party-api</module>
        <module>SopParty-Velocity</module>
        <module>SopParty-Bukkit</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>
        <!-- Targets Paper/Spigot 1.16.5 servers (PLUGIN_DEVELOPMENT_RULES: 1.16.5 first).
             io.papermc.paper:paper-api:1.16.5-R0.1-SNAPSHOT is not reliably resolvable from Maven; Bukkit modules use org.spigotmc:spigot-api plus provided Adventure. -->
        <paper.bukkit.version>1.16.5-R0.1-SNAPSHOT</paper.bukkit.version>
        <adventure.version>4.7.0</adventure.version>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>spigotmc-snapshots</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        <!-- Mirrors historic Paper artifacts (PLUGIN_DEVELOPMENT_RULES: Paper ecosystem alignment with SopLib repos). -->
        <repository>
            <id>codemc-repo</id>
            <url>https://repo.codemc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
